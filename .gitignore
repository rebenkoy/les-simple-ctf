# runtime flag file (written from the FLAG env by each eval Dockerfile)
flag.txt

# handout captures are published here as challenge files (<slug>/*.pcap), built by the
# private solutions repo's builders. they carry only sniffable creds, never a flag
# (flags live on the live services), so they're safe to commit as public handouts.
dist/

# real secret env files — only the *.env.example templates are tracked
*.env

# python
__pycache__/
*.pyc
