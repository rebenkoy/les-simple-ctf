# runtime flag file (written from the FLAG env by each eval Dockerfile)
flag.txt

# never publish captures here — pcaps are generated by the private solutions repo's
# builders and handed out via the hub, not committed to the public challenge source
*.pcap
dist/

# real secret env files — only the *.env.example templates are tracked
*.env

# python
__pycache__/
*.pyc
