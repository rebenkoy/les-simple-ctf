name: eval-calculator-hardened
services:
  calc:
    build: .
    container_name: eval-calculator-hardened
    hostname: calc-hardened
    ports:
      - "8082:80"
    environment:
      FLAG: "${FLAG:-}"
    restart: unless-stopped
