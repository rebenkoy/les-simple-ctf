# Documents the env this vault expects. Normally `just challenge hidden-vault` injects FLAG
# from the single store (infra/web/challenges.toml). Fill this in only for direct compose runs.
# FLAG must match the `flag` of the hidden-vault tile in challenges.toml.
FLAG=
